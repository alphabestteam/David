Commands:
    docker load -i mysql.tar                    -load image

    docker run -d -p 3306:3306 \                -run container
    -e MYSQL_ALLOW_EMPTY_PASSWORD=yes \
    -e MYSQL_ROOT_HOST='%' \
    -e MYSQL_DATABASE=davidDB \
    --name mysql_container \
    mysql

    docker stop mysql_container                 -stop container

    docker rm mysql_container                   -remove container

    docker run -d -p 3306:3306 \                -run container again
    -e MYSQL_ALLOW_EMPTY_PASSWORD=yes \
    -e MYSQL_ROOT_HOST='%' \
    -e MYSQL_DATABASE=davidDB \
    --name mysql_container \
    mysql